[package]
name = "murmur-protocol-v2"
version = "0.1.0"
edition = "2021"
description = "Gossip protocol for rooms to share vibes — the murmur network of the Grand Pattern"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
